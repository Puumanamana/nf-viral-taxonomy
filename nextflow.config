params {
    chunks = 100
    outdir = "$PWD/viral-annotation"
}

process {
    maxForks = 3
}

executor {
    queueSize = 3
}

singularity {
   enabled = true
   cacheDir = "${HOME}/.singularity_images.cache"
}

